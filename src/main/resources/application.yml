spring.thymeleaf.cache: false

server:
    port: 80

spring:
    datasource:
      url:
      username:
      password:
      driver-class-name: com.mysql.jdbc.Driver

    jpa:
      database: mysql
      show-sql: true
      hibernate:
        ddl-auto: update